# TypeScript Conventions

<!-- TODO: Fill in real conventions. Delete sections that don't apply. -->

## Style

- <!-- e.g. Prefer `const` over `let`; avoid `var`. -->
- <!-- e.g. Prefer `interface` over `type` for object shapes. -->
- <!-- e.g. Enable strict null checks. -->

## Patterns

<!-- Add project-specific patterns: error handling, naming, etc. -->

## Avoid

<!-- List anti-patterns observed in this repo. -->
